#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_speech-rule-engine@3.1.1@speech-rule-engine/bin/sre" "$@"
  ret=$?
else 
  node  "$basedir/../../../_speech-rule-engine@3.1.1@speech-rule-engine/bin/sre" "$@"
  ret=$?
fi
exit $ret
